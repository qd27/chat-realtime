# Port server sẽ chạy (Railway/Render tự set biến này, không cần chỉnh)
PORT=4000

# Domain(s) của frontend được phép kết nối, cách nhau bởi dấu phẩy
# Ví dụ khi đã deploy: https://your-app.vercel.app
CLIENT_URL=http://localhost:5173
